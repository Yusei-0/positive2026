import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.positive2026.app',
  appName: 'Positive 2026',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
